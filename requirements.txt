# ============================================================
#  Unified environment — medical-app-review pipeline
#  Target : Python 3.13 (Windows x64), RTX 5090 (Blackwell sm_120)
#  Covers : 01_collect / 02_preprocess / 03_sentiment / revised_results
#
#  IMPORTANT — install order matters:
#    torch is intentionally NOT listed here.
#    Install it FIRST from the CUDA 12.8 index, otherwise pip may
#    resolve a CPU-only torch that cannot use the RTX 5090:
#
#      pip install torch --index-url https://download.pytorch.org/whl/cu128
#
#    Then:  pip install -r requirements.txt
# ============================================================

# --- Sentiment scoring (03): HuggingFace zero-shot DeBERTa-v3 ---
# Pinned to match the original torch-cu121 env so pipeline behavior
# (tokenization, entailment scoring) is reproducible if 03 is re-run.
transformers==4.55.0
tokenizers==0.21.4
huggingface-hub==0.34.3
safetensors==0.6.1
accelerate==1.10.0
sentencepiece==0.2.1        # 0.2.0 has NO py3.13 wheel -> source build fails on Windows.
                            # 0.2.1 adds py3.13 wheels; tokenization output is identical.
protobuf>=5.27,<6           # needed by the DeBERTa-v2 tokenizer conversion path

# --- Scraping (01) ---
google-play-scraper==1.2.7  # exact pin: scraper breaks when Google changes endpoints

# --- GPT classification (02) ---
openai==1.99.6              # matches the SDK used for client.chat.completions.create
regex==2025.7.34            # used by text_preprocessing.strip_emoji

# --- Statistics & analysis (03 OLS, revised_results) ---
# Ranges, not exact pins: OLS/robust-SE are pure linear algebra, so the
# reported numbers are version-invariant. Exact versions get frozen into
# requirements.lock.txt after a verified install (see setup guide).
numpy>=2.1,<3
pandas>=2.2,<3
scipy>=1.14,<2
scikit-learn>=1.5,<2
statsmodels>=0.14.2,<0.15
matplotlib>=3.9,<4
seaborn>=0.13,<0.14

# --- Notebook runtime (to run the .ipynb files) ---
jupyterlab>=4.2,<5
ipykernel>=6.29,<7
ipywidgets>=8.1,<9
tqdm>=4.66,<5

# --- I/O helpers ---
openpyxl>=3.1,<4            # pandas .to_excel / .read_excel backend
